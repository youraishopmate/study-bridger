'use client';import {useState} from 'react';import {PageShell,Cards} from '../../components/PageShell'
export default function Matched(){const [step,setStep]=useState(0);const qs=['What are you hoping to study?','What is your academic background?','What budget feels realistic?','Where would you like to go?','What kind of work sounds meaningful?','When would you like to start?'];if(step===6)return <PageShell title="Your first shortlist." sub="Three routes that line up with what you told us."><section className="section"><div className="wrap"><Cards items={['University of Amsterdam','University of Toronto','University of Melbourne'].map((x,i)=>({title:x,description:'Strong course fit, welcoming student community, and a clear path to your goals.',meta:i?'Good match':'Strong match',href:'/universities/'+['amsterdam','toronto','melbourne'][i]}))}/></div></section></PageShell>;return <section className="section"><div className="wrap" style={{maxWidth:720}}><div className="eyebrow">Personalised matching</div><h1>Find your next bridge.</h1><p className="lead">A few thoughtful questions. No right answers.</p><div className="card"><small className="meta">Question {step+1} of 6</small><h2>{qs[step]}</h2><div style={{display:'grid',gap:10}}>{['Computer science and technology','Business and social sciences','Design, arts, and media','Health and life sciences'].map(x=><button className="button" key={x} onClick={()=>setStep(step+1)} style={{textAlign:'left'}}>{x}</button>)}</div></div></div></section>}
