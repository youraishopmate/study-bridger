import {PageShell,Cards} from '../../../../components/PageShell'
export default async function Country({params}:{params:{continent:string;country:string}}){const n=params.country.split('-').map(x=>x[0].toUpperCase()+x.slice(1)).join(' ');return <PageShell title={`Study in ${n}.`} sub="The essentials for planning your next move — from costs to courses."><section className="section"><div className="wrap"><div className="steps"><div className="step"><strong>€9–28k</strong><br/>Typical annual tuition</div><div className="step"><strong>3–5 yrs</strong><br/>Common degree length</div><div className="step"><strong>High</strong><br/>Student support</div></div><h2 style={{marginTop:60}}>Top universities</h2><Cards items={['University of Amsterdam','University of Toronto','University of Melbourne'].map((x,i)=>({title:x,description:`A globally minded institution in ${n}.`,meta:'University',href:'/universities/'+['amsterdam','toronto','melbourne'][i]}))}/><h2 style={{marginTop:70}}>Frequently asked questions</h2><details className="card"><summary>How much should I budget?</summary><p>Plan for tuition plus housing, transport, food, insurance, and a buffer.</p></details></div></section></PageShell>}
