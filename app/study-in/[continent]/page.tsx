import {PageShell,Cards} from '../../../components/PageShell'
const countries:Record<string,string[]>= {europe:['Germany','Netherlands','Ireland'],asia:['Japan','South Korea','Singapore'],'north-america':['Canada','United States','Mexico'],oceania:['Australia','New Zealand','Fiji'],africa:['South Africa','Morocco','Rwanda'],'south-america':['Argentina','Chile','Brazil']}
export default async function Continent({params}:{params:{continent:string}}){const name=params.continent.split('-').map(x=>x[0].toUpperCase()+x.slice(1)).join(' ');return <PageShell title={`Study in ${name}.`} sub={`A starting point for finding the right country, course, and community across ${name}.`}><section className="section"><div className="wrap"><Cards items={(countries[params.continent]||countries.europe).map(x=>({title:x,description:`Explore universities, costs, culture, and courses in ${x}.`,meta:'Country',href:`/study-in/${params.continent}/${x.toLowerCase().replaceAll(' ','-')}`}))}/><div className="blog-layout" style={{marginTop:60}}><div><div className="eyebrow">Why study here</div><h2>Choose a place that supports the life around your degree.</h2><p className="lead">The right destination gives your curiosity room to grow.</p></div><div className="card"><h3>Not sure where to start?</h3><p>Answer a few questions and we’ll turn the possibilities into a short list.</p><a className="button" href="/get-matched">Find my matches</a></div></div></div></section></PageShell>}
