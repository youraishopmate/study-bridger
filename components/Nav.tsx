'use client'
import Link from 'next/link'
import {Moon, Sun, Menu} from 'lucide-react'
import {useEffect,useState} from 'react'
export default function Nav(){const [dark,setDark]=useState(false);const [open,setOpen]=useState(false);useEffect(()=>{const saved=localStorage.getItem('sb-theme');setDark(saved==='dark');document.documentElement.dataset.theme=saved||'light'},[]);function toggle(){const next=!dark;setDark(next);document.documentElement.dataset.theme=next?'dark':'light';localStorage.setItem('sb-theme',next?'dark':'light')}return <header className="site-nav"><div className="wrap nav-inner"><Link className="logo" href="/"><span className="logo-mark">S</span>StudyBridger</Link><nav className={open?'navlinks mobile-open':'navlinks'}><Link href="/study-in/europe">Destinations</Link><Link href="/about">How it works</Link><Link href="/blog">Blog</Link><Link href="/news">News</Link><Link href="/about">About</Link></nav><div className="actions"><button className="theme" onClick={toggle} aria-label="Change theme">{dark?<Sun size={17}/>:<Moon size={17}/>}</button><Link className="button" href="/get-matched">Get matched</Link><button className="menu" onClick={()=>setOpen(!open)} aria-label="Open menu"><Menu size={18}/></button></div></div></header>}
